'use client';

// SearchBar is now embedded inline in MovieSearch.tsx.
// This file is retained as a no-op to avoid import errors if referenced elsewhere.
export default function SearchBar() { return null; }
